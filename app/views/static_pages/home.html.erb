<% provide(:title, 'Home') %>

<h1 class="center">Personal Finance App</h1>

<div id="chart"></div>
<div id="chart2"></div>

<table class="table">
 <thead>
    <tr>
      <th>¥ Assets</th>
      <th>$ Assets</th>
      <th>Converted Assets</th>
      <th>¥ Liabilities</th>
      <th>$ Liabilities</th>
      <th>Converted Liabilities</th>
      <th>¥ Equity</th>
      <th>$ Equity</th>
      <th>Equity</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= number_to_currency(0 - yen_assets, :unit => "¥", :precision => 0) %></td>
      <td><%= number_to_currency(0 - dollar_assets) %></td>
      <td><%= number_to_currency(0 - converted_assets, :unit => "¥", :precision => 0) %></td>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

<script>
var url1 = '/graph1.json';
var url2 = '/graph2.json';

var width = 960,
    height = 500,
    radius = Math.min(width, height) / 2;

var color = d3.scale.ordinal()
    .range(["#98abc5", "#8a89a6", "#7b6888", "#6b486b", "#a05d56", "#d0743c", "#ff8c00"]);

var arc = d3.svg.arc()
    .outerRadius(radius - 10)
    .innerRadius(radius - 70);

var pie = d3.layout.pie()
    .value(function(d) { return d.amount; });

var svg = d3.select("#chart").append("svg")
    .attr("width", width)
    .attr("height", height)
  .append("g")
    .attr("transform", "translate(" + width / 2 + "," + height / 2 + ")");

d3.json(url1, function(error, data) {
  console.log(data)
  data.forEach(function(d) {
    d.amount = +d.amount;
  });

  var g = svg.selectAll(".arc")
      .data(pie(data))
    .enter().append("g")
      .attr("class", "arc");

  g.append("path")
      .attr("d", arc)
      .style("fill", function(d) { return color(d.data.category.name); });

  g.append("text")
      .attr("transform", function(d) { return "translate(" + arc.centroid(d) + ")"; })
      .attr("dy", ".35em")
      .style("text-anchor", "middle")
      .text(function(d) { return d.data.category.name; });

});

var svg2 = d3.select("#chart2").append("svg")
    .attr("width", width)
    .attr("height", height)
  .append("g")
    .attr("transform", "translate(" + width / 2 + "," + height / 2 + ")");

d3.json(url2, function(error, data) {
  console.log(data)
  data.forEach(function(d) {
    d.amount = +d.amount;
  });

  var g = svg2.selectAll(".arc")
      .data(pie(data))
    .enter().append("g")
      .attr("class", "arc");

  g.append("path")
      .attr("d", arc)
      .style("fill", function(d) { return color(d.data.group_id); });

  g.append("text")
      .attr("transform", function(d) { return "translate(" + arc.centroid(d) + ")"; })
      .attr("dy", ".35em")
      .style("text-anchor", "middle")
      .text(function(d) { return d.data.group_id; });

});

</script>
<div class="well">
  <%= form_for @account, :html => {:class => "form-horizontal"}  do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
      <fieldset>
        <div class="well">

          <div class="control-group">
          <%= f.label :name, "Account Name:", :class => "control-label" %>
            <div class="controls">
              <%= f.text_field :name, :class => "input-xxlarge" %>
            </div>
          </div>  

          <div class="control-group">
          <%= f.label :owner, "Account Owner:", :class => "control-label" %>
            <div class="controls">
              <%= f.text_field :owner, :class => "input-xxlarge" %>
            </div>
          </div>    

          <div class="control-group">
            <%= f.label :internal, "Personal Account?:", :class => "control-label" %>
            <div class="controls">
              <%= f.select :internal, options_for_select([['No', false], ['Yes', true]]), {}, {:class => "span2 personal_select"} %>
            </div>
          </div>
          <span class="help-block">If account is not personal, set liquid to 'No'. If account is personal, set liquid to 'Yes' only if able to turn to cash in 24 hours or less.</span>
          <br>
          <div class="control-group">
            <%= f.label  :liquid, "Liquid?:", :class => "control-label" %>
            <div class="controls">
              <%= f.select :liquid, options_for_select([['No', false], ['Yes', true]]), {}, {:class => "span2 liquid_select"} %>
            </div>
          </div>

          <div class="control-group">
          <%= f.label :phone_number, "Phone Number:", :class => "control-label" %>
            <div class="controls">
              <%= f.text_field :phone_number, :class => "input-xxlarge" %>
            </div>
          </div>  
          
          <div class="control-group">
          <%= f.label :address, "Address:", :class => "control-label" %>
            <div class="controls">
              <%= f.text_area :address, :rows => "5", :class => "input-xxlarge" %>
            </div>
          </div>    

          <div class="control-group">
          <%= f.label :url, "URL:", :class => "control-label" %>
            <div class="controls">
              <%= f.text_field :url, :class => "input-xxlarge" %>
            </div>
          </div>    
     
        </div>  
        <div class="center">
          <%= f.submit "Create Account", :class => "btn btn-large btn-primary" %>
        </div>  
    </fieldset>
  <% end %>
</div>  
<script>
$(function() { 
  $(".personal_select").select2({
    placeholder: 'Personal Account?',
    allowClear: true
  });
  $(".liquid_select").select2({
    placeholder: 'Liquid?',
    allowClear: true
  });
});
</script>